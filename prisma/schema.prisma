generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Valuation {
  id        String   @id
  shareSlug String   @unique
  createdAt DateTime @default(now())
  payload   Json
}
